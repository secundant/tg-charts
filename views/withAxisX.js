/**
 * @param svg
 * @param {ViewBoxModel} viewBox
 */
export function withAxisX(svg, viewBox) {
  let prevFirst;
  let prevLast;
  let prevVisibility;

  const getNextDatesList = (first, last) => {

  };

  viewBox.subscribe(() => {
    if (viewBox.firstIndex === prevFirst && viewBox.lastIndex === prevLast)
      });
}
